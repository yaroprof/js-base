// --- splice method - The splice() method is used either adds/removes items to/from an array, and then returns the removed item. The first argument specifies the array position for insertion or deletion whereas the optional second argument indicates the number of elements to be deleted. Each additional argument is added to the array.

//let originArr = [1,2,3,4,5]
//let newArr = originArr.splice(3, 2)
//console.log(originArr); // [ 1, 2, 3 ]
//console.log(newArr);  // [ 4, 5 ]
